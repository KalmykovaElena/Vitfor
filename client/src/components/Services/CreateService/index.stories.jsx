import { CreateService } from './index';

export const CreateServiceComponent = {
  args: {},
};
const CreateServiceDefaultExport = {
  component: CreateService,
};
export default CreateServiceDefaultExport;
